# Лабораторная работа №2: Разработка лексического анализатора (сканера)

**Цель работы:** Изучить назначение и принципы работы лексического анализатора в структуре компилятора. Спроектировать алгоритм (диаграмму состояний) и выполнить программную реализацию сканера для выделения лексем из входного текста. Интегрировать разработанный модуль в ранее созданный графический интерфейс языкового процессора.

## 📝 Постановка задачи

Необходимо разработать лексический анализатор (сканер), который:

1. **Принимает** на вход многострочный текст (исходный код программы)
2. **Выделяет** все допустимые лексемы согласно варианту задания
3. **Классифицирует** лексемы по типам (ключевое слово, идентификатор, число, оператор, разделитель)
4. **Обнаруживает ошибки** - символы, не соответствующие ни одному типу лексем
5. **Учитывает многострочность** входного текста
6. **Интегрируется** в существующее приложение с графическим интерфейсом
7. **Выводит результаты** в таблицу с колонками:
   - Условный код
   - Тип лексемы
   - Лексема
   - Местоположение (строка, начальная и конечная позиция)
8. **Обеспечивает навигацию** по ошибкам (клик по ошибке → курсор на позицию)

## ⚙️ Вариант задания

### Номер варианта: 0 (Пример)

### Допустимые лексемы:

| Категория | Лексемы | Примеры |
|-----------|---------|---------|
| **Ключевые слова** | `int`, `float`, `if`, `else`, `while` | `int x = 5;` |
| **Идентификаторы** | буквы (лат.) + цифры, начинаются с буквы | `x`, `sum1`, `tempValue`, `counter` |
| **Целые числа** | последовательности цифр | `123`, `0`, `42`, `1000` |
| **Операторы** | `=`, `+`, `-`, `*`, `/`, `==`, `!=`, `<`, `>`, `<=`, `>=` | `a = b + c;` |
| **Разделители** | пробел, табуляция, `;`, `(`, `)`, `{`, `}`, `,`, `\n` | `if (x > 0) { y = x; }` |

### Примеры корректных входных строк:

1. `int x = 10;`
2. `if (sum1 > 0) { result = result + 1; }`
3. `float price = 99; while (price > 0) { price = price - 10; }`

## 🔄 Диаграмма состояний

```mermaid
stateDiagram-v2
    [*] --> START
    
    START --> ID_KEYWORD: буква
    START --> NUMBER: цифра
    START --> OPERATOR_SINGLE: '+', '-', '*', '/', ';', '(', ')'
    START --> OPERATOR_ASSIGN: '='
    START --> OPERATOR_COMPARE: '<', '>', '!'
    START --> DELIMITER: пробел, табуляция, '\n'
    START --> ERROR: другой символ
    
    ID_KEYWORD --> ID_KEYWORD: буква, цифра
    ID_KEYWORD --> FINISH: не буква/цифра
    
    NUMBER --> NUMBER: цифра
    NUMBER --> FINISH: не цифра
    
    OPERATOR_SINGLE --> FINISH: любой символ
    
    OPERATOR_ASSIGN --> OPERATOR_EQUAL: '='
    OPERATOR_ASSIGN --> FINISH: не '='
    
    OPERATOR_COMPARE --> OPERATOR_COMPARE_EQUAL: '='
    OPERATOR_COMPARE --> FINISH: не '='
    
    OPERATOR_EQUAL --> FINISH: любой символ
    OPERATOR_COMPARE_EQUAL --> FINISH: любой символ
    
    DELIMITER --> DELIMITER: пробел, табуляция, '\n'
    DELIMITER --> FINISH: не разделитель
    
    ERROR --> FINISH: любой символ
    
    FINISH --> [*]
